# Example environment file for local development only.
# Copy this file to `.env` and replace placeholder values as needed.
# Never commit real secrets.

# Browser-visible values for Vite (available via import.meta.env in client code).
VITE_MESOSPHERE_API_URL="http://localhost:8000"
VITE_MESOSPHERE_API_KEY="local-dev-key" # Local/dev placeholder only. Never use a real secret here.

# Server-side values for CLI/backend usage.
# Only variables prefixed with VITE_ are inlined into the browser bundle; unprefixed vars like MESOSPHERE_API_KEY remain server-side.
# The VITE_ inlining warning does NOT apply to MESOSPHERE_API_KEY. Do not create VITE_MESOSPHERE_API_KEY for real secrets because import.meta.env would expose it in the browser.
MESOSPHERE_API_URL="http://localhost:8000"
MESOSPHERE_API_KEY="local-dev-key"
