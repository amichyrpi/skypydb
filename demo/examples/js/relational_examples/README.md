## Relational Example

This directory contains the relational example file.

For local development, copy `.env.example` to `.env` and update values:
`cp .env.example .env`

To learn more, see the docs at `https://docs.ahen-studio.com/`.
