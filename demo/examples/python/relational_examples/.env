# This file is used to configure the Mesosphere API server.
# It is loaded by the server when it starts.
# You can override these values by setting environment variables.
# This file is not tracked by git by default but for the sake of this example, it is included.
# See the documentation for more information.

# The URL of the Mesosphere API server.
MESOSPHERE_API_URL="http://localhost:8000" # local instance of Mesosphere API server. If you use the CLI app, the URL will be automatically set to : https://ahen-studio.com/mesosphere.

# NOTE: The `VITE_` browser inlining warning does NOT apply to MESOSPHERE_API_KEY.
# MESOSPHERE_API_KEY is unprefixed, remains server-side, and is for local/dev only in this example.
# Never add a VITE_ prefix to real secrets (for example, do not create VITE_MESOSPHERE_API_KEY); keep production keys server-side or behind a proxy.
MESOSPHERE_API_KEY="local-dev-key"
