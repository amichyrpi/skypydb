# Custom OIDC Provider Authentication Example App

This example shows how to integrate a Custom OIDC Provider authentication into your application.

# Running the app

```shell
npm install
npx mesosphere dev
npm run dev
```
