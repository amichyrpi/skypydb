{
  "name": "mesosphere_monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,mdx}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.9.0",
    "dprint": "^0.53.0",
    "prettier": "^3.7.4",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/@mesosphere/*",
    "packages/@beyond-mesosphere/*"
  ]
}
