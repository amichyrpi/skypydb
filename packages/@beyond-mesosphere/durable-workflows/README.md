# Mesosphere Durable Workflows submodule

Durable Workflows is our own component for building durable workflows in Mesosphere. It allows you to build long-running processes that survive server restarts and handle failures gracefully with Mesosphere.

## Installation

```bash
npm install @beyond-mesosphere/durable-workflows
```
