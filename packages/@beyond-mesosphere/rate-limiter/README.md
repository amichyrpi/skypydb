# Mesosphere Rate Limiter submodule

Rate Limiter is our own component for building rate limited apps in Mesosphere. It provides a type-safe transactional application layer for rate limiting, it supports both fixed window and token bucket rate limiting.

## Installation

```bash
npm install @beyond-mesosphere/rate-limiter
```
