# Mesosphere WorkPool submodule

WorkPool is our own submodule for managing parallel execution of side actions and morph functions. It organizes them into separate customizable queues with the ability to configure the parallelism limit.

## Installation

```bash
npm install @beyond-mesosphere/workpool
```
